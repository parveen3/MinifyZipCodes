ZipCodeValidator.java - source code which has logic to verify and combine ZipCode ranges.
To run this program, checkout the git repo, import the project as maven project in Eclipse. Run as Java Application.

ZipCodeValidatorTest - has testcases to run the testcases.
Run as Junit testcases.
